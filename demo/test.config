configure primitive test ocf:pacemaker:Dummy \
  params 2: rule #ra-version version:gt 1.0 fake=real \
  params 1: fake=fake envfile=/tmp/test.env \
  op start \
    op_meta 2: rule #ra-version version:gt 1.0 timeout=120s \
    op_meta 1: timeout=60s
